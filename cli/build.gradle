version = "1.0"

dependencies {
  compile project(":sum")
}

jar {
  manifest {
    attributes 'Main-Class': 'ru.nsychev.intro17.cli.Main'
  }

  from {
    configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
  }
}

