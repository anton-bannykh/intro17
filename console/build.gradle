version = "1.1"

dependencies {
  compile project(":sum")
}

jar {
  manifest {
    attributes 'Main-Class': 'spolutrean.intro17.console.Main'
  }

  from {
    configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
  }
}
 